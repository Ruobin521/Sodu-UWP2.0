<Page
    x:Class="Sodu.View.OnlineContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sodu.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:Sodu.ViewModel"
    xmlns:control="using:Sodu.Control"
    DataContext="{Binding  Instance.OnlineBookContent,Source={StaticResource Locator}}"
    mc:Ignorable="d">
    <Grid  >
        <ScrollViewer>
            <TextBlock Text="{Binding ContentText}" TextWrapping="Wrap" LineHeight="30" Foreground="Black" FontSize="20"></TextBlock>
        </ScrollViewer>

        <control:CustomLoadingControl ></control:CustomLoadingControl>
    </Grid>
</Page>
